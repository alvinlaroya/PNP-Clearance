export const allCase = (state) => state.allCase;
export const caseBadges = (state) => state.badges;
export const relatedCase = (state) => state.relatedCase;
export const hasRelatedCase = (state) => state.hasRelatedCase;
